# 数据库说明

数据库结构由 `backend/src/main/resources/schema.sql` 维护，首次启动 MySQL 容器时会自动初始化。

公开仓库不提交全量数据库导出。全量导出可能包含用户口令哈希、审计记录、内部地址、工作流业务数据及加密后的连接器凭证，不适合作为示例数据公开。需要迁移环境时，请按 `docs/04-部署升级与回滚手册.md` 在受控位置创建和保存备份。
